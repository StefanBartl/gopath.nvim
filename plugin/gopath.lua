-- Auto-loaded by Neovim. Bootstraps the gopath.nvim plugin.
-- All commands and keymaps are registered inside require("gopath").setup().
-- This file only ensures the plugin is not double-loaded.
if vim.g.loaded_gopath then return end
vim.g.loaded_gopath = true
